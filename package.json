{
  "name": "fully-involved-pour",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd web && npm ci && npm run build && cd .. && mkdir -p dist/server dist/.openai && cp -R web/dist/. dist && cp .openai/hosting.json dist/.openai/hosting.json && cp sites-server/index.js dist/server/index.js",
    "test:rules": "firebase emulators:exec --only firestore \"cd web && npx vitest run --config vitest.rules.config.ts\""
  }
}
